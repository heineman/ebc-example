language: java

dist: xenial

services:
   - xvfb

before_script:
   - "export DISPLAY=:99.0"
   - "sh -e /etc/init.d/xvfb start"
   - sudo apt-get install ant-optional
   - sleep 3   # give xvfb some time to start
